[project]
name = "elastic2clp-json"
version = "0.1.0"
description = "Ingest from elasticsearch into clp-json"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pymysql>=1.1.2",
    "pyyaml>=6.0.3",
    "requests>=2.32.5",
]
